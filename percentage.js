let num = prompt ("Enter a Percentage") 
    if (num >= 90) {
        console.log ("A Grade");
    } else if (num >= 80) {
        console.log ("B Grade");
    } else if (num >= 70) {
        console.log ("C Grade");
    } else if (num >= 60) {
        console.log ("D Grade");
    } else {
        console.log ("Fail");
    }